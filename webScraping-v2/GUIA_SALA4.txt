======================================================================
         GUIA DE INSTALACION Y EJECUCION - SALA 4 (CLIP)
======================================================================

1. REQUISITOS A INSTALAR
----------------------------------------------------------------------
Abre la terminal en la carpeta principal e instala únicamente 
las librerias necesarias para el procesamiento de imagenes e IA:

   pip install torch torchvision transformers pillow numpy


2. ARCHIVOS DEL MODULO (SALA 4)
----------------------------------------------------------------------
Asegurate de que tus archivos esten ubicados en estas rutas:

   webScraping-v2/
   │
   ├── data/
   │   ├── images_final/             <- Carpeta con las imagenes 
   │   └── embeddings_productos.npy <- Archivo .npy generado con los vectores
   │
   ├── generar_embeddings.py         <- Script 1: Convierte imagenes a vectores
   ├── buscar_por_imagen.py          <- Script 2: Busca por similitud coseno (Top 5)
   └── GUIA_SALA4.txt                <- Este archivo de guia


3. ORDEN DE EJECUCION
----------------------------------------------------------------------

   PASO 1: Generar la base de vectores (Embeddings)
   Procesa las imagenes con el modelo CLIP (openai/clip-vit-base-patch32) 
   y crea el archivo binario 'data/embeddings_productos.npy':

      python generar_embeddings.py


   PASO 2: Probar la busqueda visual por similitud
   Envia una imagen de prueba como argumento para obtener las 5 mas parecidas:

      python buscar_por_imagen.py data/images_final/AIM-P001-001.jpg


4. VERIFICACION RAPIDA DE VECTORES (OPCIONAL)
----------------------------------------------------------------------
Para comprobar desde la terminal que el archivo .npy contiene los 100 
vectores generados correctamente sin abrir archivos extra:

   python -c "import numpy as np; data = np.load('data/embeddings_productos.npy', allow_pickle=True).item(); print('Total imágenes:', len(data)); img = list(data.keys())[0]; print(f'Vector de {img} (primeros 5 números):', data[img][:5])"

======================================================================